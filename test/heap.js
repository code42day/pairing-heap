var should = require('should');
var code42dayPairingHeap = require('../');

describe('code42day-pairing-heap node module', function () {
  it('must have at least one test', function () {
    code42dayPairingHeap();
    should.fail('Need to write tests.');
  });
});
